import os
'''Store global settings here
'''

# Absolute path to where static files (for testing) are stored
STATIC_DIR = os.path.abspath(os.path.join(os.path.dirname(os.path.dirname(__file__)), 'tests'))
